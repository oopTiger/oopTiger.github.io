
<!DOCTYPE html>
<html lang="en">
<head>
<!--
  ╔══════════════════════════════════════════════════════════╗
  ║          LIVE REVENUE TRACKER — 30/60/90 Day Plan        ║
  ║                                                          ║
  ║  Host on GitHub Pages:                                   ║
  ║  1. Create a new repo (e.g. "revenue-tracker")           ║
  ║  2. Upload this file as index.html                       ║
  ║  3. Go to Settings → Pages → Deploy from main branch     ║
  ║  4. Your live URL: https://USERNAME.github.io/REPONAME   ║
  ║                                                          ║
  ║  Customise targets in the Settings panel on the page.    ║
  ║  Settings are saved in your browser automatically.       ║
  ╚══════════════════════════════════════════════════════════╝
-->
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Revenue Tracker — 30/60/90 Day Plan</title>
<meta name="description" content="Live revenue tracker with daily, monthly, quarterly and yearly goal progress.">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=DM+Mono:ital,wght@0,300;0,400;0,500;1,300&family=Sora:wght@300;400;500;600&display=swap" rel="stylesheet">

<style>
/* ─── TOKENS ─────────────────────────────────────────── */
:root {
  --bg:      #05080D;
  --surf:    #0B1018;
  --card:    #0F1620;
  --border:  #182030;
  --border2: #243040;
  --teal:    #00D4AA;
  --teal2:   #00A882;
  --gold:    #F5A623;
  --blue:    #4A9EFF;
  --red:     #FF4D6A;
  --text:    #E2EAF4;
  --sub:     #8899AA;
  --muted:   #445566;
  --dim:     #1A2535;
}

/* ─── RESET & BASE ───────────────────────────────────── */
*, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

html { scroll-behavior: smooth; }

body {
  background: var(--bg);
  color: var(--text);
  font-family: 'Sora', sans-serif;
  min-height: 100vh;
  overflow-x: hidden;
  cursor: default;
}

/* ─── AMBIENT BACKGROUND ────────────────────────────── */
.bg-glow {
  position: fixed; inset: 0; pointer-events: none; z-index: 0;
  background:
    radial-gradient(ellipse 60% 40% at 20% 0%, rgba(0,212,170,0.055) 0%, transparent 70%),
    radial-gradient(ellipse 40% 30% at 80% 100%, rgba(74,158,255,0.04) 0%, transparent 70%);
}

.grid-overlay {
  position: fixed; inset: 0; pointer-events: none; z-index: 0; opacity: 0.025;
  background-image:
    linear-gradient(var(--teal) 1px, transparent 1px),
    linear-gradient(90deg, var(--teal) 1px, transparent 1px);
  background-size: 40px 40px;
}

/* ─── LAYOUT ─────────────────────────────────────────── */
.page {
  position: relative; z-index: 1;
  max-width: 1100px;
  margin: 0 auto;
  padding: 32px 20px 60px;
}

/* ─── TOP NAV ────────────────────────────────────────── */
.topnav {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 36px;
  flex-wrap: wrap;
  gap: 16px;
}

.brand {
  display: flex;
  align-items: center;
  gap: 14px;
}

.brand-icon {
  width: 42px; height: 42px;
  background: linear-gradient(135deg, var(--teal), var(--teal2));
  border-radius: 10px;
  display: flex; align-items: center; justify-content: center;
  font-size: 20px;
  box-shadow: 0 0 20px rgba(0,212,170,0.3);
}

.brand-text h1 {
  font-family: 'Bebas Neue', sans-serif;
  font-size: 26px;
  letter-spacing: 3px;
  color: var(--text);
  line-height: 1;
}

.brand-text p {
  font-family: 'DM Mono', monospace;
  font-size: 10px;
  color: var(--muted);
  letter-spacing: 2px;
  text-transform: uppercase;
  margin-top: 3px;
}

.clock-block { text-align: right; }

.clock-block .time {
  font-family: 'DM Mono', monospace;
  font-size: 32px;
  font-weight: 500;
  color: var(--teal);
  letter-spacing: 3px;
  line-height: 1;
}

.clock-block .date {
  font-family: 'DM Mono', monospace;
  font-size: 11px;
  color: var(--muted);
  letter-spacing: 1px;
  margin-top: 4px;
}

/* ─── SETTINGS PANEL ─────────────────────────────────── */
.settings-toggle {
  background: var(--surf);
  border: 1px solid var(--border);
  border-radius: 10px;
  padding: 10px 18px;
  color: var(--sub);
  font-family: 'DM Mono', monospace;
  font-size: 11px;
  letter-spacing: 1.5px;
  text-transform: uppercase;
  cursor: pointer;
  display: flex;
  align-items: center;
  gap: 8px;
  transition: border-color 0.2s, color 0.2s;
  margin-bottom: 16px;
  width: fit-content;
}
.settings-toggle:hover { border-color: var(--teal); color: var(--teal); }
.settings-toggle .arrow { transition: transform 0.3s; display: inline-block; }
.settings-toggle.open .arrow { transform: rotate(180deg); }

.settings-panel {
  background: var(--surf);
  border: 1px solid var(--border);
  border-radius: 14px;
  padding: 20px 24px 24px;
  margin-bottom: 28px;
  display: none;
  animation: fadeSlide 0.2s ease;
}

.settings-panel.visible { display: block; }

@keyframes fadeSlide {
  from { opacity: 0; transform: translateY(-8px); }
  to   { opacity: 1; transform: translateY(0); }
}

.settings-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
  gap: 16px;
  margin-bottom: 18px;
}

.field label {
  display: block;
  font-family: 'DM Mono', monospace;
  font-size: 10px;
  color: var(--muted);
  letter-spacing: 1.5px;
  text-transform: uppercase;
  margin-bottom: 7px;
}

.field input {
  width: 100%;
  background: var(--bg);
  border: 1px solid var(--dim);
  border-radius: 8px;
  padding: 10px 13px;
  color: var(--text);
  font-family: 'DM Mono', monospace;
  font-size: 14px;
  outline: none;
  transition: border-color 0.2s, box-shadow 0.2s;
}

.field input:focus {
  border-color: var(--teal);
  box-shadow: 0 0 0 3px rgba(0,212,170,0.08);
}

.settings-actions { display: flex; gap: 10px; flex-wrap: wrap; }

.btn-save {
  background: var(--teal);
  color: #000;
  border: none;
  border-radius: 8px;
  padding: 11px 28px;
  font-family: 'Sora', sans-serif;
  font-size: 13px;
  font-weight: 600;
  cursor: pointer;
  letter-spacing: 0.3px;
  transition: opacity 0.2s, transform 0.1s;
}
.btn-save:hover { opacity: 0.88; }
.btn-save:active { transform: scale(0.97); }

.btn-reset {
  background: transparent;
  color: var(--muted);
  border: 1px solid var(--border2);
  border-radius: 8px;
  padding: 11px 20px;
  font-family: 'Sora', sans-serif;
  font-size: 13px;
  cursor: pointer;
  transition: border-color 0.2s, color 0.2s;
}
.btn-reset:hover { border-color: var(--red); color: var(--red); }

.save-msg {
  font-family: 'DM Mono', monospace;
  font-size: 11px;
  color: var(--teal);
  align-self: center;
  opacity: 0;
  transition: opacity 0.3s;
}
.save-msg.show { opacity: 1; }

/* ─── HERO CARD ──────────────────────────────────────── */
.hero {
  background: var(--surf);
  border: 1px solid var(--border);
  border-radius: 18px;
  padding: 32px 36px;
  margin-bottom: 20px;
  display: grid;
  grid-template-columns: 1fr auto;
  gap: 24px;
  align-items: center;
  position: relative;
  overflow: hidden;
  transition: border-color 0.4s;
}

.hero.live { border-color: rgba(0,212,170,0.35); }

.hero::before {
  content: '';
  position: absolute;
  top: 0; left: 0; right: 0; height: 1px;
  background: linear-gradient(90deg, var(--teal), rgba(0,212,170,0.2), transparent);
  opacity: 0;
  transition: opacity 0.4s;
}
.hero.live::before { opacity: 1; }

.hero-eyebrow {
  font-family: 'DM Mono', monospace;
  font-size: 10px;
  color: var(--muted);
  letter-spacing: 2.5px;
  text-transform: uppercase;
  margin-bottom: 10px;
  display: flex;
  align-items: center;
  gap: 8px;
}

.live-dot {
  width: 7px; height: 7px;
  border-radius: 50%;
  background: var(--teal);
  animation: blink 1.2s step-end infinite;
}

@keyframes blink { 50% { opacity: 0; } }

.hero-amount {
  font-family: 'Bebas Neue', sans-serif;
  font-size: 80px;
  letter-spacing: 2px;
  color: var(--teal);
  line-height: 0.95;
  transition: color 0.3s;
}

.hero-sub {
  font-family: 'DM Mono', monospace;
  font-size: 12px;
  color: var(--sub);
  margin-top: 10px;
}

.hero-sub span { color: var(--text); }

.hero-right {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 12px;
}

.stat-chip {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 10px;
  padding: 14px 20px;
  text-align: center;
  min-width: 130px;
}

.stat-chip .sc-val {
  font-family: 'DM Mono', monospace;
  font-size: 22px;
  font-weight: 500;
  color: var(--gold);
  letter-spacing: 1px;
}

.stat-chip .sc-lbl {
  font-size: 10px;
  color: var(--muted);
  letter-spacing: 1px;
  text-transform: uppercase;
  margin-top: 3px;
}

.rate-pill {
  font-family: 'DM Mono', monospace;
  font-size: 12px;
  color: var(--sub);
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 8px;
  padding: 6px 14px;
}
.rate-pill span { color: var(--teal); }

/* ─── GOAL CARDS ─────────────────────────────────────── */
.goals-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
  margin-bottom: 20px;
}

.goal-card {
  background: var(--surf);
  border: 1px solid var(--border);
  border-radius: 16px;
  padding: 22px 22px 18px;
  position: relative;
  overflow: hidden;
  transition: border-color 0.3s, transform 0.2s;
}

.goal-card:hover { transform: translateY(-2px); border-color: var(--border2); }

.goal-card::after {
  content: '';
  position: absolute;
  top: 0; left: 0; right: 0; height: 2px;
}

.goal-card.monthly::after  { background: linear-gradient(90deg, var(--blue), transparent); }
.goal-card.quarterly::after{ background: linear-gradient(90deg, var(--gold), transparent); }
.goal-card.yearly::after   { background: linear-gradient(90deg, var(--red), transparent); }

.gc-label {
  font-family: 'DM Mono', monospace;
  font-size: 10px;
  letter-spacing: 2px;
  text-transform: uppercase;
  margin-bottom: 14px;
}

.goal-card.monthly  .gc-label { color: var(--blue); }
.goal-card.quarterly .gc-label { color: var(--gold); }
.goal-card.yearly   .gc-label { color: var(--red); }

.gc-earned {
  font-family: 'Bebas Neue', sans-serif;
  font-size: 42px;
  letter-spacing: 1px;
  line-height: 1;
  margin-bottom: 5px;
}

.goal-card.monthly  .gc-earned { color: var(--blue); }
.goal-card.quarterly .gc-earned { color: var(--gold); }
.goal-card.yearly   .gc-earned { color: var(--red); }

.gc-remaining {
  font-family: 'DM Mono', monospace;
  font-size: 11px;
  color: var(--muted);
  margin-bottom: 16px;
}

.gc-remaining span { color: var(--sub); }

.progress-track {
  background: var(--dim);
  border-radius: 100px;
  height: 4px;
  overflow: visible;
  margin-bottom: 8px;
  position: relative;
}

.progress-fill {
  height: 100%;
  border-radius: 100px;
  transition: width 1.2s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
}

.progress-fill::after {
  content: '';
  position: absolute;
  right: -1px; top: 50%;
  transform: translateY(-50%);
  width: 10px; height: 10px;
  border-radius: 50%;
  background: inherit;
  box-shadow: 0 0 8px currentColor;
}

.goal-card.monthly  .progress-fill { background: var(--blue); }
.goal-card.quarterly .progress-fill { background: var(--gold); }
.goal-card.yearly   .progress-fill { background: var(--red); }

.gc-pct {
  font-family: 'DM Mono', monospace;
  font-size: 11px;
  color: var(--muted);
  text-align: right;
}

/* ─── STATS STRIP ────────────────────────────────────── */
.stats-strip {
  background: var(--surf);
  border: 1px solid var(--border);
  border-radius: 14px;
  padding: 18px 24px;
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 0;
  margin-bottom: 20px;
}

.ss-item {
  padding: 4px 20px;
  border-right: 1px solid var(--border);
}

.ss-item:first-child { padding-left: 0; }
.ss-item:last-child  { border-right: none; }

.ss-lbl {
  font-family: 'DM Mono', monospace;
  font-size: 9px;
  color: var(--muted);
  letter-spacing: 1.5px;
  text-transform: uppercase;
  margin-bottom: 6px;
}

.ss-val {
  font-family: 'DM Mono', monospace;
  font-size: 16px;
  font-weight: 500;
  color: var(--text);
}

.ss-val.teal  { color: var(--teal); }
.ss-val.gold  { color: var(--gold); }
.ss-val.green { color: #4ADE80; }

/* ─── DAYS LEFT ROW ──────────────────────────────────── */
.days-row {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
  margin-bottom: 20px;
}

.day-card {
  background: var(--surf);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 16px 20px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.day-card .dc-lbl {
  font-family: 'DM Mono', monospace;
  font-size: 10px;
  color: var(--muted);
  letter-spacing: 1.5px;
  text-transform: uppercase;
}

.day-card .dc-val {
  font-family: 'Bebas Neue', sans-serif;
  font-size: 28px;
  letter-spacing: 1px;
}

.day-card.month   .dc-val { color: var(--blue); }
.day-card.quarter .dc-val { color: var(--gold); }
.day-card.year    .dc-val { color: var(--red); }

/* ─── FOOTER ─────────────────────────────────────────── */
.footer {
  text-align: center;
  padding-top: 20px;
  border-top: 1px solid var(--border);
}

.footer p {
  font-family: 'DM Mono', monospace;
  font-size: 10px;
  color: var(--muted);
  letter-spacing: 1px;
}

.footer a { color: var(--teal); text-decoration: none; }
.footer a:hover { text-decoration: underline; }

/* ─── RESPONSIVE ─────────────────────────────────────── */
@media (max-width: 768px) {
  .hero { grid-template-columns: 1fr; }
  .hero-right { align-items: flex-start; flex-direction: row; flex-wrap: wrap; }
  .hero-amount { font-size: 60px; }
  .goals-grid { grid-template-columns: 1fr; }
  .stats-strip { grid-template-columns: repeat(2, 1fr); }
  .ss-item:nth-child(2) { border-right: none; }
  .days-row { grid-template-columns: 1fr; }
}

@media (max-width: 480px) {
  .hero-amount { font-size: 48px; }
  .stats-strip { grid-template-columns: 1fr 1fr; }
}

/* ─── ENTER ANIMATIONS ───────────────────────────────── */
.fade-in {
  opacity: 0;
  transform: translateY(16px);
  animation: fadeIn 0.5s ease forwards;
}

.fade-in:nth-child(1) { animation-delay: 0.05s; }
.fade-in:nth-child(2) { animation-delay: 0.1s; }
.fade-in:nth-child(3) { animation-delay: 0.15s; }
.fade-in:nth-child(4) { animation-delay: 0.2s; }
.fade-in:nth-child(5) { animation-delay: 0.25s; }

@keyframes fadeIn {
  to { opacity: 1; transform: translateY(0); }
}
</style>
</head>

<body>
<div class="bg-glow"></div>
<div class="grid-overlay"></div>

<div class="page">

  <!-- ── TOP NAV ── -->
  <div class="topnav fade-in">
    <div class="brand">
      <div class="brand-icon">📈</div>
      <div class="brand-text">
        <h1>REVENUE TRACKER</h1>
        <p>30 · 60 · 90 Day Plan</p>
      </div>
    </div>
    <div class="clock-block">
      <div class="time" id="clock">00:00:00</div>
      <div class="date" id="dateLine">—</div>
    </div>
  </div>

  <!-- ── SETTINGS TOGGLE ── -->
  <button class="settings-toggle" id="settingsToggle" onclick="toggleSettings()">
    ⚙ Settings <span class="arrow">▼</span>
  </button>

  <!-- ── SETTINGS PANEL ── -->
  <div class="settings-panel" id="settingsPanel">
    <div class="settings-grid">
      <div class="field">
        <label>Daily Target ($)</label>
        <input type="number" id="dailyTarget" placeholder="3333">
      </div>
      <div class="field">
        <label>Monthly Target ($)</label>
        <input type="number" id="monthlyTarget" placeholder="100000">
      </div>
      <div class="field">
        <label>Quarterly Target ($)</label>
        <input type="number" id="quarterlyTarget" placeholder="280000">
      </div>
      <div class="field">
        <label>Yearly Target ($)</label>
        <input type="number" id="yearlyTarget" placeholder="1200000">
      </div>
      <div class="field">
        <label>Work Start (24h)</label>
        <input type="time" id="workStart" value="09:00">
      </div>
      <div class="field">
        <label>Work End (24h)</label>
        <input type="time" id="workEnd" value="17:00">
      </div>
    </div>
    <div class="settings-actions">
      <button class="btn-save" onclick="saveSettings()">Save Settings</button>
      <button class="btn-reset" onclick="resetSettings()">Reset to Default</button>
      <span class="save-msg" id="saveMsg">✓ Saved to browser</span>
    </div>
  </div>

  <!-- ── HERO: TODAY EARNED ── -->
  <div class="hero fade-in" id="heroBlock">
    <div class="hero-left">
      <div class="hero-eyebrow">
        <span class="live-dot"></span>
        Today's Earnings
      </div>
      <div class="hero-amount" id="todayEarned">$0</div>
      <div class="hero-sub" id="todaySub">
        <span id="todayLeft">—</span> remaining of today's target
      </div>
    </div>
    <div class="hero-right">
      <div class="stat-chip">
        <div class="sc-val" id="workTimeEl">0h 0m</div>
        <div class="sc-lbl">Time Elapsed</div>
      </div>
      <div class="rate-pill">Rate: <span id="rateEl">$0/hr</span></div>
    </div>
  </div>

  <!-- ── GOAL CARDS ── -->
  <div class="goals-grid fade-in">
    <div class="goal-card monthly">
      <div class="gc-label">Monthly Goal</div>
      <div class="gc-earned" id="mEarned">$0</div>
      <div class="gc-remaining">Remaining: <span id="mRemaining">—</span></div>
      <div class="progress-track"><div class="progress-fill" id="mBar" style="width:0%"></div></div>
      <div class="gc-pct" id="mPct">0%</div>
    </div>
    <div class="goal-card quarterly">
      <div class="gc-label">Quarterly Goal</div>
      <div class="gc-earned" id="qEarned">$0</div>
      <div class="gc-remaining">Remaining: <span id="qRemaining">—</span></div>
      <div class="progress-track"><div class="progress-fill" id="qBar" style="width:0%"></div></div>
      <div class="gc-pct" id="qPct">0%</div>
    </div>
    <div class="goal-card yearly">
      <div class="gc-label">Yearly Goal</div>
      <div class="gc-earned" id="yEarned">$0</div>
      <div class="gc-remaining">Remaining: <span id="yRemaining">—</span></div>
      <div class="progress-track"><div class="progress-fill" id="yBar" style="width:0%"></div></div>
      <div class="gc-pct" id="yPct">0%</div>
    </div>
  </div>

  <!-- ── STATS STRIP ── -->
  <div class="stats-strip fade-in">
    <div class="ss-item">
      <div class="ss-lbl">Per Minute</div>
      <div class="ss-val teal" id="perMin">$0.00</div>
    </div>
    <div class="ss-item">
      <div class="ss-lbl">Per Hour</div>
      <div class="ss-val teal" id="perHour">$0.00</div>
    </div>
    <div class="ss-item">
      <div class="ss-lbl">Needed / Day (Month)</div>
      <div class="ss-val gold" id="neededDay">—</div>
    </div>
    <div class="ss-item">
      <div class="ss-lbl">On Track?</div>
      <div class="ss-val" id="onTrack">—</div>
    </div>
  </div>

  <!-- ── DAYS LEFT ROW ── -->
  <div class="days-row fade-in">
    <div class="day-card month">
      <div class="dc-lbl">Days Left in Month</div>
      <div class="dc-val" id="daysMonth">—</div>
    </div>
    <div class="day-card quarter">
      <div class="dc-lbl">Days Left in Quarter</div>
      <div class="dc-val" id="daysQ">—</div>
    </div>
    <div class="day-card year">
      <div class="dc-lbl">Days Left in Year</div>
      <div class="dc-val" id="daysY">—</div>
    </div>
  </div>

  <!-- ── FOOTER ── -->
  <div class="footer fade-in">
    <p>Built for the 30 · 60 · 90 Day Finance Plan &nbsp;|&nbsp; Settings auto-save to your browser &nbsp;|&nbsp; Updates every second</p>
  </div>

</div><!-- /page -->

<script>
// ════════════════════════════════════════════════════════
//  REVENUE TRACKER — CONFIGURATION & ENGINE
//  Targets and work hours are saved in localStorage so
//  they persist between browser sessions automatically.
// ════════════════════════════════════════════════════════

const DEFAULTS = {
  dailyTarget:     3333,
  monthlyTarget:   100000,
  quarterlyTarget: 280000,
  yearlyTarget:    1200000,
  workStartHour:   9,
  workStartMin:    0,
  workEndHour:     17,
  workEndMin:      0,
};

let cfg = { ...DEFAULTS };

// ── PERSISTENCE ──────────────────────────────────────────
function loadSettings() {
  try {
    const saved = localStorage.getItem('revenueTrackerCfg');
    if (saved) cfg = { ...DEFAULTS, ...JSON.parse(saved) };
  } catch(e) {}
  syncInputs();
}

function syncInputs() {
  document.getElementById('dailyTarget').value     = cfg.dailyTarget;
  document.getElementById('monthlyTarget').value   = cfg.monthlyTarget;
  document.getElementById('quarterlyTarget').value = cfg.quarterlyTarget;
  document.getElementById('yearlyTarget').value    = cfg.yearlyTarget;
  document.getElementById('workStart').value =
    String(cfg.workStartHour).padStart(2,'0') + ':' + String(cfg.workStartMin).padStart(2,'0');
  document.getElementById('workEnd').value =
    String(cfg.workEndHour).padStart(2,'0') + ':' + String(cfg.workEndMin).padStart(2,'0');
}

function saveSettings() {
  cfg.dailyTarget     = parseFloat(document.getElementById('dailyTarget').value)     || DEFAULTS.dailyTarget;
  cfg.monthlyTarget   = parseFloat(document.getElementById('monthlyTarget').value)   || DEFAULTS.monthlyTarget;
  cfg.quarterlyTarget = parseFloat(document.getElementById('quarterlyTarget').value) || DEFAULTS.quarterlyTarget;
  cfg.yearlyTarget    = parseFloat(document.getElementById('yearlyTarget').value)    || DEFAULTS.yearlyTarget;

  const [sh, sm] = document.getElementById('workStart').value.split(':').map(Number);
  const [eh, em] = document.getElementById('workEnd').value.split(':').map(Number);
  cfg.workStartHour = sh; cfg.workStartMin = sm;
  cfg.workEndHour   = eh; cfg.workEndMin   = em;

  try { localStorage.setItem('revenueTrackerCfg', JSON.stringify(cfg)); } catch(e) {}

  const msg = document.getElementById('saveMsg');
  msg.classList.add('show');
  setTimeout(() => msg.classList.remove('show'), 2500);
  tick();
}

function resetSettings() {
  cfg = { ...DEFAULTS };
  syncInputs();
  try { localStorage.removeItem('revenueTrackerCfg'); } catch(e) {}
  tick();
}

// ── SETTINGS PANEL TOGGLE ────────────────────────────────
function toggleSettings() {
  const panel  = document.getElementById('settingsPanel');
  const toggle = document.getElementById('settingsToggle');
  const isOpen = panel.classList.contains('visible');
  panel.classList.toggle('visible', !isOpen);
  toggle.classList.toggle('open', !isOpen);
}

// ── FORMATTERS ───────────────────────────────────────────
function fmt(n) {
  if (n >= 1_000_000) return '$' + (n / 1_000_000).toFixed(2) + 'M';
  if (n >= 1_000)     return '$' + (n / 1_000).toFixed(1) + 'K';
  return '$' + n.toFixed(2);
}

function fmtComma(n) {
  return '$' + Math.round(n).toLocaleString('en-US');
}

function daysInMonth(y, m) { return new Date(y, m + 1, 0).getDate(); }

function dayOfYear(d) {
  return Math.floor((d - new Date(d.getFullYear(), 0, 0)) / 86_400_000);
}

function daysInYear(y) {
  return ((y % 4 === 0 && y % 100 !== 0) || y % 400 === 0) ? 366 : 365;
}

function quarterInfo(now) {
  const q      = Math.floor(now.getMonth() / 3);
  const qStart = new Date(now.getFullYear(), q * 3, 1);
  const qEnd   = new Date(now.getFullYear(), q * 3 + 3, 0);
  const total  = Math.round((qEnd - qStart) / 86_400_000) + 1;
  const elapsed= Math.round((now - qStart) / 86_400_000);
  return { elapsed, remaining: total - elapsed, total };
}

// ── MAIN TICK ────────────────────────────────────────────
function tick() {
  const now = new Date();

  // Clock
  document.getElementById('clock').textContent =
    now.toLocaleTimeString('en-US', { hour12: false });
  document.getElementById('dateLine').textContent =
    now.toLocaleDateString('en-US', { weekday: 'long', month: 'long', day: 'numeric', year: 'numeric' });

  // Work window math
  const startSec    = cfg.workStartHour * 3600 + cfg.workStartMin * 60;
  const endSec      = cfg.workEndHour   * 3600 + cfg.workEndMin   * 60;
  const totalWork   = endSec - startSec;
  const nowSec      = now.getHours() * 3600 + now.getMinutes() * 60 + now.getSeconds();
  const isWorking   = nowSec >= startSec && nowSec <= endSec;

  let elapsedSec = 0;
  if (isWorking)        elapsedSec = nowSec - startSec;
  else if (nowSec > endSec) elapsedSec = totalWork;

  const dayProg     = Math.min(elapsedSec / totalWork, 1);
  const todayEarned = cfg.dailyTarget * dayProg;
  const todayLeft   = Math.max(cfg.dailyTarget - todayEarned, 0);

  // Hero
  document.getElementById('todayEarned').textContent = fmtComma(todayEarned);
  document.getElementById('todayLeft').textContent   = fmtComma(todayLeft);

  const eh = Math.floor(elapsedSec / 3600);
  const em = Math.floor((elapsedSec % 3600) / 60);
  document.getElementById('workTimeEl').textContent = eh + 'h ' + em + 'm';

  const hrRate = elapsedSec > 0 ? (todayEarned / (elapsedSec / 3600)) : 0;
  document.getElementById('rateEl').textContent = fmt(hrRate) + '/hr';

  const hero = document.getElementById('heroBlock');
  hero.classList.toggle('live', isWorking);

  // Monthly
  const dim      = daysInMonth(now.getFullYear(), now.getMonth());
  const dom      = now.getDate();
  const mElapsed = dom - 1 + dayProg;
  const mEarned  = (mElapsed / dim) * cfg.monthlyTarget;
  const mLeft    = Math.max(cfg.monthlyTarget - mEarned, 0);
  const mPct     = Math.min(mEarned / cfg.monthlyTarget * 100, 100);
  document.getElementById('mEarned').textContent    = fmtComma(mEarned);
  document.getElementById('mRemaining').textContent = fmtComma(mLeft);
  document.getElementById('mBar').style.width       = mPct.toFixed(1) + '%';
  document.getElementById('mPct').textContent       = mPct.toFixed(1) + '%';

  // Quarterly
  const qi      = quarterInfo(now);
  const qEarned = ((qi.elapsed + dayProg) / qi.total) * cfg.quarterlyTarget;
  const qLeft   = Math.max(cfg.quarterlyTarget - qEarned, 0);
  const qPct    = Math.min(qEarned / cfg.quarterlyTarget * 100, 100);
  document.getElementById('qEarned').textContent    = fmtComma(qEarned);
  document.getElementById('qRemaining').textContent = fmtComma(qLeft);
  document.getElementById('qBar').style.width       = qPct.toFixed(1) + '%';
  document.getElementById('qPct').textContent       = qPct.toFixed(1) + '%';

  // Yearly
  const doy     = dayOfYear(now) - 1 + dayProg;
  const diy     = daysInYear(now.getFullYear());
  const yEarned = (doy / diy) * cfg.yearlyTarget;
  const yLeft   = Math.max(cfg.yearlyTarget - yEarned, 0);
  const yPct    = Math.min(yEarned / cfg.yearlyTarget * 100, 100);
  document.getElementById('yEarned').textContent    = fmtComma(yEarned);
  document.getElementById('yRemaining').textContent = fmtComma(yLeft);
  document.getElementById('yBar').style.width       = yPct.toFixed(1) + '%';
  document.getElementById('yPct').textContent       = yPct.toFixed(1) + '%';

  // Stats strip
  const perMin = elapsedSec > 0 ? todayEarned / (elapsedSec / 60) : 0;
  document.getElementById('perMin').textContent  = fmt(perMin) + '/min';
  document.getElementById('perHour').textContent = fmt(hrRate) + '/hr';

  const dLeft         = dim - dom;
  const neededPerDay  = dLeft > 0 ? mLeft / dLeft : mLeft;
  document.getElementById('neededDay').textContent = fmtComma(neededPerDay) + '/day';

  const expected  = (dom / dim) * cfg.monthlyTarget;
  const onTrack   = mEarned >= expected * 0.9;
  const otEl      = document.getElementById('onTrack');
  otEl.textContent = onTrack ? '✅ On Track' : '⚠️ Behind';
  otEl.style.color = onTrack ? 'var(--teal)' : 'var(--gold)';

  // Days left cards
  document.getElementById('daysMonth').textContent = dLeft;
  document.getElementById('daysQ').textContent     = qi.remaining;
  document.getElementById('daysY').textContent     = diy - dayOfYear(now);
}

// ── INIT ────────────────────────────────────────────────
loadSettings();
tick();
setInterval(tick, 1000);
</script>
</body>
</html>
